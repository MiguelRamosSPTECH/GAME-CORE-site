var database = require("../database/config");

function criarLayout(nomeLayout,ListaChecked,fk_empresa_cargo){
    console.log("model config")

    // pegar vcalores de alerta

// inserir nome do layout e fk emopresa

// for + select (componente e metrica) pra pegar cada componente e metrica

// insert na tabela config do server

    let 




}

module.exports = {
    criarLayout
};